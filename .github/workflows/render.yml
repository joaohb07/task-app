name: Render
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Wait for Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Wait for Render Deployment
        uses: bounceapp/render-action@0.6.0
        with:
          service-id: srv-cfikv4hgp3jh03klg9qg
          retries: 20
          wait: 16000
          sleep: 30000
